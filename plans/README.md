# plans

The project's planning source of truth.

| File | What |
|------|------|
| `agent_checklist.md` | **Task SSOT.** One `###` subsection = one branch; phases P0→P7 across Waves 0–3. Appendix A = canonical contract conventions; Appendix B = exact gate commands. |
| `checklist_flow.md` | Staged **parallel** execution map (stage → parallel subsections → merge order → running tally) for the `checklist-phase-runner-parallel` skill. |
| `first_pass_high_level_plan.md` | The 200-foot view: goal, architecture, build phases, scope/non-goals. |

Detailed design lives in `docs/2026-05-24-data-connectors-and-frontend-design.md`; layout in `docs/STRUCTURE.md`. Work top-to-bottom; mark `- [x]` only when a subsection's **Verify** passes.
